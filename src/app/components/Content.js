export function Content() {
    const $content = document.createElement('div')
    $content.innerHTML = `<p> soy el contenido principal que se debe cargar en cada una de las distitas secciones del menu</p>`
    return $content  
}
